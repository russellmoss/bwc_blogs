var fs=require("fs");
var out=[];
function p(s){out.push(s===undefined?"":String(s));}
var q=String.fromCharCode(96);
var B3=q+q+q;
var B3ts=q+q+q+"ts";
var DQ=String.fromCharCode(34);
var SQ=String.fromCharCode(39);
p("# Pattern Finder Findings -- Guide 3 Reference");
p("");
p("Generated: 2026-03-01");
p("Scope: All Guide 1 and Guide 2 patterns for consistent reuse in Guide 3 (Onyx RAG Integration).");
p("");
p("---");
p("");
p("## 1. API Route Handler Pattern");
p("");
p("### Files Analyzed");
p("- src/app/api/health/route.ts");
p("- src/app/api/auth/[...nextauth]/route.ts");
p("- src/app/api/users/route.ts and users/[id]/route.ts");
p("- src/app/api/content-map/route.ts and content-map/[id]/route.ts");
p("- src/app/api/content-map/import/route.ts");
p("");
p("### Entry Point to Data Flow");
p("");
p(B3);
p("HTTP request arrives");
p("  -> middleware (withAuth) blocks unauthenticated requests at the edge");
p("  -> route handler: try { ... } catch (error) { ... }");
p("  -> await requireRole(...) -- FIRST statement in try");
p("  -> await request.json() -- read raw body");
p("  -> SomeSchema.safeParse(body) -- Zod, never .parse()");
p("  -> if (!parsed.success) return 400 VALIDATION_ERROR");
p("  -> business logic (Prisma queries, lib function calls)");
p("  -> return NextResponse.json({ success: true, data: ... })");
p("  -> catch: classify error string -> 401 / 403 / 500");
p(B3);
p("");
p("### Canonical Import Block");
p("");
p(B3ts);
p("import { NextRequest, NextResponse } from "+DQ+"next/server"+DQ+";");
p("import { prisma } from "+DQ+"@/lib/db"+DQ+";");
p("import { requireRole } from "+DQ+"@/lib/auth/session"+DQ+";");
p("import { z } from "+DQ+"zod"+DQ+";");
p(B3);
p("");
p("Rules:");
p("- NextRequest: only when handler reads request.json(). Unused request is prefixed _request.");
p("- NextResponse: ALWAYS imported in every route file.");
p("- prisma: always from @/lib/db -- never new PrismaClient() inside a route.");
p("- requireRole: always from @/lib/auth/session.");
p("- Zod schemas: INLINE at top of route file, NOT in a separate schema file.");
p("");
p("### Zod Validation -- always .safeParse(), never .parse()");
p("");
p(B3ts);
p("const parsed = SomeSchema.safeParse(body);");
p("if (!parsed.success) {");
p("  return NextResponse.json(");
p("    {");
p("      success: false,");
p("      error: {");
p("        code: "+DQ+"VALIDATION_ERROR"+DQ+",");
p("        message: "+DQ+"Invalid input"+DQ+",");
p("        details: parsed.error.flatten(),");
p("      },");
p("    },");
p("    { status: 400 }");
p("  );");
p("}");
p(B3);
p("");
p("The details field ALWAYS carries parsed.error.flatten() on validation failures.");
p("");
p("### Success Response Format");
p("");
p(B3ts);
p("return NextResponse.json({ success: true, data: entries });                 // GET list");
p("return NextResponse.json({ success: true, data: entry });                   // GET one, PATCH, DELETE");
p("return NextResponse.json({ success: true, data: entry }, { status: 201 });  // POST create");
p(B3);
p("");
p("Data is ALWAYS under the data key. Status 201 only for POST creates.");
p("");
p("### Try/Catch Structure -- canonical, copy this exactly");
p("");
p(B3ts);
p("export async function GET() {");
p("  try {");
p("    await requireRole("+DQ+"admin"+DQ+", "+DQ+"editor"+DQ+", "+DQ+"viewer"+DQ+");");
p("    // ... business logic");
p("    return NextResponse.json({ success: true, data: result });");
p("  } catch (error) {");
p("    const message = error instanceof Error ? error.message : "+DQ+"Unknown error"+DQ+";");
p("    if (message === "+DQ+"AUTH_REQUIRED"+DQ+") {");
p("      return NextResponse.json(");
p("        { success: false, error: { code: "+DQ+"AUTH_REQUIRED"+DQ+", message: "+DQ+"Authentication required"+DQ+" } },");
p("        { status: 401 }");
p("      );");
p("    }");
p("    if (message === "+DQ+"AUTH_FORBIDDEN"+DQ+") {");
p("      return NextResponse.json(");
p("        { success: false, error: { code: "+DQ+"AUTH_FORBIDDEN"+DQ+", message: "+DQ+"Admin access required"+DQ+" } },");
p("        { status: 403 }");
p("      );");
p("    }");
p("    return NextResponse.json(");
p("      { success: false, error: { code: "+DQ+"INTERNAL_ERROR"+DQ+", message } },");
p("      { status: 500 }");
p("    );");
p("  }");
p("}");
p(B3);
p("");
p("Rules:");
p("- Entire handler body is inside one try/catch.");
p("- requireRole() is FIRST in try -- before reading body or running any logic.");
p("- Catch order: AUTH_REQUIRED (401) -> AUTH_FORBIDDEN (403) -> INTERNAL_ERROR (500).");
p("- Error extraction: const message = error instanceof Error ? error.message : "+DQ+"Unknown error"+DQ);
p("  This EXACT line appears verbatim in every catch block across all 7 route files.");
p("- No re-throwing. The catch block always returns a NextResponse.");
p("");
p("### Dynamic Route Params (Next.js 15)");
p("");
p(B3ts);
p("export async function GET(");
p("  _request: NextRequest,");
p("  { params }: { params: Promise<{ id: string }> }  // Promise required in Next.js 15");
p(") {");
p("  try {");
p("    await requireRole("+DQ+"admin"+DQ+", "+DQ+"editor"+DQ+", "+DQ+"viewer"+DQ+");");
p("    const { id } = await params;  // always await params before destructuring");
p("  }");
p("}");
p(B3);
p("");
p("### Prisma Select Pattern");
p("");
p(B3ts);
p("// Define at module scope -- reused by all handlers in the file:");
p("const myModelSelect = {");
p("  id: true, fieldA: true, createdAt: true, updatedAt: true,");
p("};");
p("");
p("const entries = await prisma.myModel.findMany({");
p("  select: myModelSelect,");
p("  orderBy: { createdAt: "+DQ+"desc"+DQ+" },");
p("}); ");
p(B3);
p("");
p("No findMany() or findUnique() without explicit select. Consistent across all 7 route files.");
p("Route handlers call prisma.* DIRECTLY without retryDatabaseOperation.");
p("retryDatabaseOperation is ONLY used in src/lib/auth/config.ts (the login flow).");
p("");
p("---");
p("");
p("## 2. Lib Module Pattern");
p("");
p("### Directory Layout");
p("");
p(B3);
p("src/lib/");
p("  auth/config.ts       -- NextAuth authOptions config object");
p("  auth/session.ts      -- requireAuth, requireRole, getCurrentUser, SessionUser type");
p("  auth/password.ts     -- hashPassword, verifyPassword, validatePassword");
p("  db/index.ts          -- Prisma singleton (globalForPrisma pattern)");
p("  db/retry.ts          -- retryDatabaseOperation<T> with exponential backoff");
p("  env.ts               -- central env accessor (optionalEnv pattern)");
p("  onyx/client.ts       -- config stub -> Guide 3 replaces with real async client");
p("  claude/client.ts     -- config stub");
p("  cloudinary/client.ts -- config stub");
p("  content-map/index.ts  -- barrel re-export (multi-file module example)");
p("  content-map/import.ts -- parseCSV, mapCSVRow, importToDatabase, seedCorePages");
p("  content-map/slug.ts   -- generateSlug, ensureUniqueSlug");
p(B3);
p("");
p("### Service Config Stub Pattern (verbatim -- all three stubs share the same shape)");
p("");
p(B3ts);
p("// src/lib/onyx/client.ts (current stub)");
p("export const onyxConfig = {");
p("  apiUrl: process.env.ONYX_API_URL || "+DQ+DQ+",");
p("  apiKey: process.env.ONYX_API_KEY || "+DQ+DQ+",");
p("};");
p("");
p("// src/lib/claude/client.ts");
p("export const claudeConfig = {");
p("  apiKey: process.env.ANTHROPIC_API_KEY || "+DQ+DQ+",");
p("  model: process.env.ANTHROPIC_MODEL || "+DQ+"claude-sonnet-4-5-20250929"+DQ+",");
p("};");
p("");
p("// src/lib/cloudinary/client.ts");
p("export const cloudinaryConfig = {");
p("  url: process.env.CLOUDINARY_URL || "+DQ+DQ+",");
p("  cloudName: process.env.CLOUDINARY_CLOUD_NAME || "+DQ+DQ+",");
p("  uploadPreset: process.env.CLOUDINARY_UPLOAD_PRESET || "+DQ+"blog"+DQ+",");
p("};");
p(B3);
p("");
p("Guide 3 REPLACES src/lib/onyx/client.ts with a real module exporting async functions.");
p("The config object shape can be kept as an internal const or folded into the new module.");
p("");
p("### Prisma Singleton (verbatim)");
p("");
p(B3ts);
p("// src/lib/db/index.ts");
p("import { PrismaClient } from "+DQ+"@prisma/client"+DQ+";");
p("const globalForPrisma = globalThis as unknown as { prisma: PrismaClient | undefined };");
p("export const prisma = globalForPrisma.prisma ?? new PrismaClient({");
p("  log: process.env.NODE_ENV === "+DQ+"development"+DQ+" ? ["+DQ+"query"+DQ+","+DQ+"error"+DQ+","+DQ+"warn"+DQ+"] : ["+DQ+"error"+DQ+"],");
p("}); ");
p("if (process.env.NODE_ENV !== "+DQ+"production"+DQ+") globalForPrisma.prisma = prisma;");
p(B3);
p("");
p("Import in routes: import { prisma } from "+DQ+"@/lib/db"+DQ);
p("");
p("### Retry Wrapper (verbatim -- model Guide 3 HTTP retry on this pattern)");
p("");
p(B3ts);
p("// src/lib/db/retry.ts");
p("export async function retryDatabaseOperation<T>(");
p("  operation: () => Promise<T>,");
p("  maxRetries: number = 3,");
p("  baseDelay: number = 500");
p("): Promise<T> {");
p("  let lastError: Error | undefined;");
p("  for (let attempt = 0; attempt <= maxRetries; attempt++) {");
p("    try { return await operation(); }");
p("    catch (error) {");
p("      lastError = error instanceof Error ? error : new Error(String(error));");
p("      const isRetryable = lastError.message.includes("+DQ+"Connection refused"+DQ+") ||");
p("        lastError.message.includes("+DQ+"Connection terminated"+DQ+") ||");
p("        lastError.message.includes("+DQ+"ECONNRESET"+DQ+") ||");
p("        lastError.message.includes("+DQ+"socket hang up"+DQ+") ||");
p("        lastError.message.includes("+DQ+"Can"+SQ+"t reach database server"+DQ+");");
p("      if (!isRetryable || attempt === maxRetries) throw lastError;");
p("      const delay = baseDelay * Math.pow(2, attempt);");
p("      await new Promise((resolve) => setTimeout(resolve, delay));");
p("    }");
p("  }");
p("  throw lastError;");
p("}");
p(B3);
p("");
p("Create a similar HTTP fetch retry wrapper for Onyx calls in src/lib/onyx/client.ts.");
p("Detect: ECONNRESET, ETIMEDOUT, ECONNREFUSED, and network fetch failures.");
p("");
p("### Barrel Index Pattern");
p("");
p(B3ts);
p("// src/lib/content-map/index.ts");
p("export { generateSlug, ensureUniqueSlug } from "+DQ+"./slug"+DQ+";");
p("export { parseCSV, mapCSVRow, importToDatabase, seedCorePages, CORE_PAGES } from "+DQ+"./import"+DQ+";");
p("export type { ContentMapRow } from "+DQ+"./import"+DQ+";");
p(B3);
p("");
p("Create src/lib/onyx/index.ts with the same barrel pattern once module has 2+ files.");
p("");
p("---");
p("");
p("## 3. Types Pattern");
p("");
p("### Files Analyzed");
p("- src/types/index.ts");
p("- src/types/api.ts");
p("- src/types/onyx.ts");
p("- src/types/auth.ts, content-map.ts, article.ts, renderer.ts, qa.ts, claude.ts, photo.ts");
p("");
p("### Index Re-Export Pattern (verbatim)");
p("");
p(B3ts);
p("// src/types/index.ts -- wildcard re-export from every type file");
p("export * from "+DQ+"./auth"+DQ+";");
p("export * from "+DQ+"./content-map"+DQ+";");
p("export * from "+DQ+"./article"+DQ+";");
p("export * from "+DQ+"./renderer"+DQ+";");
p("export * from "+DQ+"./qa"+DQ+";");
p("export * from "+DQ+"./onyx"+DQ+";");
p("export * from "+DQ+"./claude"+DQ+";");
p("export * from "+DQ+"./photo"+DQ+";");
p("export * from "+DQ+"./api"+DQ+";");
p(B3);
p("");
p("Rule: every new type file must be added here with export *.");
p("Do NOT add a named re-export here -- use wildcard only.");
p("");
p("### ApiResponse and ErrorCode (verbatim from src/types/api.ts)");
p("");
p(B3ts);
p("// Standard API response wrappers");
p("export interface ApiSuccess<T> {");
p("  success: true;");
p("  data: T;");
p("}");
p("");
p("export interface ApiError {");
p("  success: false;");
p("  error: {");
p("    code: string;");
p("    message: string;");
p("    details?: unknown;");
p("  };");
p("}");
p("");
p("export type ApiResponse<T> = ApiSuccess<T> | ApiError;");
p("");
p("// Error codes (from orchestration doc S10)");
p("export type ErrorCode =");
p("  | "+DQ+"AUTH_REQUIRED"+DQ+"");
p("  | "+DQ+"AUTH_FORBIDDEN"+DQ+"");
p("  | "+DQ+"VALIDATION_ERROR"+DQ+"");
p("  | "+DQ+"NOT_FOUND"+DQ+"");
p("  | "+DQ+"GENERATION_FAILED"+DQ+"");
p("  | "+DQ+"ONYX_UNAVAILABLE"+DQ+"   // already registered -- Guide 3 uses this code");
p("  | "+DQ+"RENDER_ERROR"+DQ+"");
p("  | "+DQ+"QA_GATE_FAILED"+DQ+"");
p("  | "+DQ+"CLOUDINARY_ERROR"+DQ+"");
p("  | "+DQ+"LINK_VERIFICATION_FAILED"+DQ+"");
p("  | "+DQ+"INTERNAL_ERROR"+DQ+";");
p(B3);
p("");
p("### Pre-Defined Onyx Types (verbatim from src/types/onyx.ts)");
p("");
p(B3ts);
p("export interface OnyxSearchResult {");
p("  documentId: string;");
p("  content: string;");
p("  sourceDocument: string; // Filename or document title");
p("  score: number;");
p("  metadata: Record<string, unknown>;");
p("}");
p("");
p("export interface OnyxContext {");
p("  query: string;");
p("  results: OnyxSearchResult[];");
p("  totalResults: number;");
p("  searchTimeMs: number;");
p("}");
p("");
p("export interface OnyxHealthStatus {");
p("  healthy: boolean;");
p("  indexedDocuments: number | null;");
p("  lastIndexTime: string | null;");
p("  responseTimeMs: number;");
p("}");
p(B3);
p("");
p("Rule: src/types/onyx.ts ALREADY EXISTS and defines the contracts Guide 3 must satisfy.");
p("Do NOT redefine OnyxSearchResult, OnyxContext, or OnyxHealthStatus -- import from @/types.");
p("");
p("---");
p("");
p("## 4. Test Script Pattern");
p("");
p("### Files Analyzed");
p("- scripts/test-guide-1.ts");
p("- scripts/test-guide-2.ts");
p("");
p("### Canonical Structure");
p("");
p(B3ts);
p("/**");
p(" * Integration test for Guide N: <Name>");
p(" *");
p(" * Run with: npx tsx scripts/test-guide-N.ts");
p(" */");
p("");
p("import { PrismaClient } from "+DQ+"@prisma/client"+DQ+";");
p("");
p("const prisma = new PrismaClient();  // direct instantiation -- NOT the singleton from @/lib/db");
p("");
p("async function test() {");
p("  let passed = 0;");
p("  let failed = 0;");
p("");
p("  function check(name: string, result: boolean, detail?: string) {");
p("    if (result) {");
p("      console.log("+SQ+"  PASS "+SQ+" + name);");
p("      passed++;");
p("    } else {");
p("      console.log("+SQ+"  FAIL "+SQ+" + name + (detail ? "+SQ+" -- "+SQ+" + detail : "+SQ+DQ+SQ+"));");
p("      failed++;");
p("    }");
p("  }");
var NL=String.fromCharCode(10);
p("  console.log(NL + "+DQ+"=== Guide N Integration Tests ==="+DQ+" + NL);");
p("");
p("  // --- Test 1: <Category> ------------------------------------------");
p("  console.log("+DQ+"1. <test group label>"+DQ+");");
p("  try {");
p("    // ... DB or logic assertions ...");
p("    check("+SQ+"<description>"+SQ+", <boolean expression>, "+SQ+"<detail>"+SQ+");");
p("  } catch (e) {");
p("    check("+SQ+"<description>"+SQ+", false, (e as Error).message);");
p("  }");
p("");
p("  // --- Test N: API endpoints (SKIP if server not running) -----------");
p("  console.log("+DQ+"N. API endpoints"+DQ+");");
p("  const appUrl = process.env.APP_URL || "+DQ+"http://localhost:3000"+DQ+";");
p("  try {");
p("    const res = await fetch(appUrl + "+DQ+"/api/some-route"+DQ+");");
p("    check("+DQ+"GET /api/some-route responds"+DQ+", res.status === 200 || res.status === 401);");
p("  } catch {");
p("    console.log("+DQ+"  SKIP API tests -- dev server not running"+DQ+");");
p("  }");
p("");
p("  // --- Summary -------------------------------------------------------");
p("  console.log(NL + "+DQ+"=== Results: "+DQ+" + passed + "+DQ+" passed, "+DQ+" + failed + "+DQ+" failed ==="+DQ+" + NL);");
p("  await prisma."+DQ+"$disconnect"+DQ+"();");
p("  process.exit(failed > 0 ? 1 : 0);");
p("}");
p("");
p("test();");
p(B3);
p("");
p("### Test Script Rules");
p("");
p("1. Run via: npx tsx scripts/test-guide-N.ts");
p("2. Direct PrismaClient instantiation -- never import the singleton from @/lib/db.");
p("3. check() signature has THREE params since Guide 2: (name, result, detail?).");
p("   Guide 1 used TWO params (no detail). Guide 3 must use the 3-param form.");
p("4. API tests wrapped in try/catch with SKIP output (not FAIL) if server not running.");
p("5. Accept 401 as valid for authenticated endpoints: res.status === 200 || res.status === 401.");
p("6. Always call prisma."+DQ+"$disconnect"+DQ+"() at the end.");
p("7. process.exit(failed > 0 ? 1 : 0) -- non-zero on any failure.");
p("8. Group tests with numbered console.log headings.");
p("");
p("### Guide 3 Test Script Must Cover");
p("");
p("1. Onyx health endpoint reachable (GET /api/onyx/health returns 200)");
p("2. Onyx search endpoint accepts a query (POST /api/onyx/search)");
p("3. OnyxContext shape has correct fields (query, results[], totalResults, searchTimeMs)");
p("4. Error handling: ONYX_UNAVAILABLE returned when Onyx is down");
p("");
p("---");
p("");
p("## 5. Error Handling Pattern");
p("");
p("### Auth Error Flow");
p("");
p("requireRole() throws plain Error objects with magic string messages.");
p("These are NOT custom error classes -- just new Error("+DQ+"AUTH_REQUIRED"+DQ+").");
p("");
p(B3ts);
p("// src/lib/auth/session.ts");
p("export async function requireAuth(): Promise<SessionUser> {");
p("  const user = await getCurrentUser();");
p("  if (!user) throw new Error("+DQ+"AUTH_REQUIRED"+DQ+");");
p("  return user;");
p("}");
p("");
p("export async function requireRole(...roles: UserRole[]): Promise<SessionUser> {");
p("  const user = await requireAuth();");
p("  if (!roles.includes(user.role)) throw new Error("+DQ+"AUTH_FORBIDDEN"+DQ+");");
p("  return user;");
p("}");
p(B3);
p("");
p("### Error Catch Block (verbatim -- identical in all 7 route files)");
p("");
p(B3ts);
p("} catch (error) {");
p("  const message = error instanceof Error ? error.message : "+DQ+"Unknown error"+DQ+";");
p("  if (message === "+DQ+"AUTH_REQUIRED"+DQ+") {");
p("    return NextResponse.json(");
p("      { success: false, error: { code: "+DQ+"AUTH_REQUIRED"+DQ+", message: "+DQ+"Authentication required"+DQ+" } },");
p("      { status: 401 }");
p("    );");
p("  }");
p("  if (message === "+DQ+"AUTH_FORBIDDEN"+DQ+") {");
p("    return NextResponse.json(");
p("      { success: false, error: { code: "+DQ+"AUTH_FORBIDDEN"+DQ+", message: "+DQ+"Admin access required"+DQ+" } },");
p("      { status: 403 }");
p("    );");
p("  }");
p("  return NextResponse.json(");
p("    { success: false, error: { code: "+DQ+"INTERNAL_ERROR"+DQ+", message } },");
p("    { status: 500 }");
p("  );");
p("}");
p(B3);
p("");
p("### Guide 3 Error Codes");
p("");
p("ONYX_UNAVAILABLE is pre-registered in src/types/api.ts ErrorCode union.");
p("Use it when the Onyx service is unreachable or returns a non-2xx status.");
p("");
p(B3ts);
p("// For Onyx-specific failures in /api/onyx/* routes:");
p("return NextResponse.json(");
p("  { success: false, error: { code: "+DQ+"ONYX_UNAVAILABLE"+DQ+", message } },");
p("  { status: 503 }");
p(");");
p(B3);
p("");
p("Status mapping:");
p("- 400: VALIDATION_ERROR");
p("- 401: AUTH_REQUIRED");
p("- 403: AUTH_FORBIDDEN");
p("- 404: NOT_FOUND");
p("- 503: ONYX_UNAVAILABLE (service dependency down)");
p("- 500: INTERNAL_ERROR (all other errors)");
p("");
p("### No Custom Error Classes");
p("");
p("No custom error class hierarchy exists. All errors are plain Error objects.");
p("Do NOT introduce custom error classes in Guide 3 -- throw new Error(string) only.");
p("");
p("---");
p("");
p("## 6. Environment Variables Pattern");
p("");
p("### Onyx Variables (.env.example)");
p("");
p(B3);
p("ONYX_BASE_URL=https://rmoss-onyx.xyz");
p("ONYX_API_URL=https://rmoss-onyx.xyz/api");
p("ONYX_API_KEY=your-onyx-api-key");
p("ONYX_INDEX_NAME=default");
p("ONYX_SEARCH_TIMEOUT_MS=10000");
p(B3);
p("");
p("### Current Onyx Stub (src/lib/onyx/client.ts)");
p("");
p(B3ts);
p("export const onyxConfig = {");
p("  apiUrl: process.env.ONYX_API_URL || "+DQ+DQ+",");
p("  apiKey: process.env.ONYX_API_KEY || "+DQ+DQ+",");
p("};");
p(B3);
p("");
p("The stub only reads ONYX_API_URL and ONYX_API_KEY.");
p("Guide 3 must add ONYX_BASE_URL, ONYX_INDEX_NAME, ONYX_SEARCH_TIMEOUT_MS to the module.");
p("");
p("### How to Read Env Vars");
p("");
p("Two styles exist in the codebase:");
p("- Direct: process.env.VARIABLE_NAME (used in service config stubs)");
p("- Central: import { env } from "+DQ+"@/lib/env"+DQ+"; env.VARIABLE_NAME");
p("");
p("env.ts ALREADY includes ONYX_API_URL and ONYX_API_KEY.");
p("env.ts is MISSING: ONYX_BASE_URL, ONYX_INDEX_NAME, ONYX_SEARCH_TIMEOUT_MS.");
p("Guide 3 should add these missing vars to env.ts.");
p("");
p("### env.ts Current State (verbatim)");
p("");
p(B3ts);
p("// src/lib/env.ts");
p("function optionalEnv(name: string, defaultValue: string = "+DQ+DQ+"): string {");
p("  return process.env[name] || defaultValue;");
p("}");
p("");
p("export const env = {");
p("  DATABASE_URL: optionalEnv("+SQ+"DATABASE_URL"+SQ+"),");
p("  ANTHROPIC_API_KEY: optionalEnv("+SQ+"ANTHROPIC_API_KEY"+SQ+"),");
p("  ANTHROPIC_MODEL: optionalEnv("+SQ+"ANTHROPIC_MODEL"+SQ+", "+SQ+"claude-sonnet-4-5-20250929"+SQ+"),");
p("  ONYX_API_URL: optionalEnv("+SQ+"ONYX_API_URL"+SQ+"),");
p("  ONYX_API_KEY: optionalEnv("+SQ+"ONYX_API_KEY"+SQ+"),");
p("  // TO ADD: ONYX_BASE_URL, ONYX_INDEX_NAME, ONYX_SEARCH_TIMEOUT_MS");
p("  BWC_SITE_URL: optionalEnv("+SQ+"BWC_SITE_URL"+SQ+", "+SQ+"https://www.bhutanwine.com"+SQ+"),");
p("};");
p(B3);
p("");
p("---");
p("");
p("## 7. Middleware and Auth Pattern");
p("");
p("### Middleware (verbatim from src/middleware.ts)");
p("");
p(B3ts);
p("import { withAuth } from "+DQ+"next-auth/middleware"+DQ+";");
p("");
p("export default withAuth({");
p("  pages: { signIn: "+DQ+"/login"+DQ+" },");
p("});");
p("");
p("export const config = {");
p("  matcher: [");
p("    "+SQ+"/((?!login|api/auth|api/health|api/capture|_next|favicon.ico).*)"+SQ+",");
p("  ],");
p("};");
p(B3);
p("");
p("Routes excluded from auth middleware:");
p("- /login");
p("- /api/auth/* (NextAuth internal)");
p("- /api/health (public health check)");
p("- /api/capture (public lead capture from Wix)");
p("- /_next, /favicon.ico (static assets)");
p("");
p("Guide 3 NEW ROUTES: /api/onyx/health and /api/onyx/search are AUTHENTICATED.");
p("Do NOT add them to the middleware exclusion list.");
p("");
p("### UserRole Type and Valid Roles");
p("");
p(B3ts);
p("// src/lib/auth/session.ts");
p("export type UserRole = "+DQ+"admin"+DQ+" | "+DQ+"editor"+DQ+" | "+DQ+"viewer"+DQ+";");
p(B3);
p("");
p("Three roles exist: admin, editor, viewer.");
p("Onyx search: allow all roles -> requireRole("+DQ+"admin"+DQ+", "+DQ+"editor"+DQ+", "+DQ+"viewer"+DQ+")");
p("Onyx health: allow all roles -> requireRole("+DQ+"admin"+DQ+", "+DQ+"editor"+DQ+", "+DQ+"viewer"+DQ+")");
p("");
p("### requireRole Usage Survey (all 7 existing routes)");
p("");
p("- GET /api/health: NO auth (public)");
p("- GET /api/users: requireRole("+DQ+"admin"+DQ+")");
p("- POST /api/users: requireRole("+DQ+"admin"+DQ+")");
p("- GET /api/users/[id]: requireRole("+DQ+"admin"+DQ+")");
p("- PATCH /api/users/[id]: requireRole("+DQ+"admin"+DQ+")");
p("- DELETE /api/users/[id]: requireRole("+DQ+"admin"+DQ+")");
p("- GET /api/content-map: requireRole("+DQ+"admin"+DQ+", "+DQ+"editor"+DQ+", "+DQ+"viewer"+DQ+")");
p("- POST /api/content-map: requireRole("+DQ+"admin"+DQ+", "+DQ+"editor"+DQ+")");
p("- GET /api/content-map/[id]: requireRole("+DQ+"admin"+DQ+", "+DQ+"editor"+DQ+", "+DQ+"viewer"+DQ+")");
p("- PATCH /api/content-map/[id]: requireRole("+DQ+"admin"+DQ+", "+DQ+"editor"+DQ+")  [note: leading empty-string arg bug]");
p("- DELETE /api/content-map/[id]: requireRole("+DQ+"admin"+DQ+")");
p("- POST /api/content-map/import: requireRole("+DQ+"admin"+DQ+")");
p("");
p("---");
p("");
p("## 8. Consistency Ratings");
p("");
p("| Pattern | Rating | Notes |");
p("|---------|--------|-------|");
p("| API route import block | CONSISTENT | Identical across all 7 route files |");
p("| Zod safeParse + VALIDATION_ERROR | CONSISTENT | Every route with a body uses this |");
p("| Success response shape | CONSISTENT | No deviations found |");
p("| Error catch block structure | CONSISTENT | Verbatim copy in all 7 files |");
p("| requireRole as first try statement | CONSISTENT | Without exception |");
p("| Prisma select (no bare findMany) | CONSISTENT | All queries use explicit select |");
p("| Dynamic params as Promise (Next.js 15) | CONSISTENT | Both [id] routes await params |");
p("| Barrel index for multi-file lib modules | CONSISTENT | content-map module follows it |");
p("| check() 3-param signature in test scripts | DRIFT | Guide 1: 2 params, Guide 2: 3 params |");
p("| env.ts as central accessor | DRIFT | Stubs use process.env directly |");
p("| retryDatabaseOperation in routes | DRIFT | Only auth/config.ts uses it |");
p("");
p("---");
p("");
p("## 9. Inconsistencies Found");
p("");
p("### 1. check() Signature Drift");
p("");
p("test-guide-1.ts: check(name: string, result: boolean)");
p("test-guide-2.ts: check(name: string, result: boolean, detail?: string)");
p("Guide 3 MUST use the 3-param form.");
p("");
p("### 2. env.ts Underused");
p("");
p("src/lib/env.ts defines optionalEnv but service stubs read process.env directly.");
p("Guide 3 should add missing Onyx vars to env.ts and import from there.");
p("");
p("### 3. CLOUDINARY_UPLOAD_PRESET Missing from .env.example");
p("");
p("src/lib/cloudinary/client.ts references this var but it is absent from .env.example.");
p("Pre-existing bug. Do not fix in Guide 3.");
p("");
p("### 4. retryDatabaseOperation Not Used in Routes");
p("");
p("The retry wrapper is only used in auth/config.ts, not in any route handler.");
p("Build a SEPARATE retryFetch wrapper for Onyx HTTP calls in src/lib/onyx/client.ts.");
p("Model it on retryDatabaseOperation but target network error strings.");
p("");
p("### 5. Extra Comma Bug in PATCH /api/content-map/[id]");
p("");
p("requireRole call has a leading empty string: requireRole("+DQ+DQ+", "+DQ+"admin"+DQ+", "+DQ+"editor"+DQ+")");
p("Likely a typo. Does not break runtime. Do not replicate.");
p("");
p("---");
p("");
p("## 10. Anti-Patterns to Avoid in Guide 3");
p("");
p("1. new PrismaClient() inside a route handler");
p("   Always use: import { prisma } from "+DQ+"@/lib/db"+DQ+"");
p("");
p("2. Zod .parse() instead of .safeParse()");
p("   Bad: MySchema.parse(body) -- throws uncaught ZodError");
p("   Good: MySchema.safeParse(body) then check parsed.success");
p("");
p("3. requireRole() called after reading the request body");
p("   Auth check MUST be the first statement in every try block");
p("");
p("4. Re-throwing errors from catch blocks");
p("   Catch always returns a NextResponse, never throws");
p("");
p("5. Returning data without the success/data envelope");
p("   Always: { success: true, data: ... }");
p("");
p("6. status 200 for POST creates");
p("   POST creates must return status 201");
p("");
p("7. Skipping await on params in Next.js 15 dynamic routes");
p("   const { id } = await params  (not: const { id } = params)");
p("");
p("8. Importing types from specific type files instead of @/types");
p("   Use: import { OnyxContext } from "+DQ+"@/types"+DQ+"");
p("");
p("9. Introducing custom error classes");
p("   Use plain: throw new Error("+DQ+"message"+DQ+")");
p("");
p("---");
p("");
p("## 11. Recommendations for Guide 3");
p("");
p("### New Files to Create");
p("");
p("| File | Purpose |");
p("|------|---------|");
p("| src/lib/onyx/client.ts | Replace stub with real async Onyx client |");
p("| src/lib/onyx/index.ts | Barrel re-export (barrel pattern) |");
p("| src/app/api/onyx/health/route.ts | Proxy Onyx health to frontend |");
p("| src/app/api/onyx/search/route.ts | Accept query, call Onyx, return OnyxContext |");
p("| scripts/test-guide-3.ts | Integration test for Guide 3 |");
p("");
p("### Files to Modify");
p("");
p("| File | Change |");
p("|------|--------|");
p("| src/lib/env.ts | Add ONYX_BASE_URL, ONYX_INDEX_NAME, ONYX_SEARCH_TIMEOUT_MS |");
p("");
p("### Files to Leave Unchanged");
p("");
p("- src/types/onyx.ts (OnyxSearchResult, OnyxContext, OnyxHealthStatus already defined)");
p("- src/types/api.ts (ONYX_UNAVAILABLE already in ErrorCode union)");
p("- src/middleware.ts (Onyx routes are authenticated -- no change needed)");
p("- src/lib/auth/session.ts (requireRole pattern stays as-is)");
p("");
p("### Canonical Onyx Route Template (follows all established patterns)");
p("");
p(B3ts);
p("// src/app/api/onyx/search/route.ts");
p("import { NextRequest, NextResponse } from "+DQ+"next/server"+DQ+";");
p("import { requireRole } from "+DQ+"@/lib/auth/session"+DQ+";");
p("import { z } from "+DQ+"zod"+DQ+";");
p("import { searchOnyx } from "+DQ+"@/lib/onyx"+DQ+";  // barrel index");
p("");
p("const SearchSchema = z.object({");
p("  query: z.string().min(1),");
p("  maxResults: z.number().int().min(1).max(20).optional().default(5),");
p("}); ");
p("");
p("export async function POST(request: NextRequest) {");
p("  try {");
p("    await requireRole("+DQ+"admin"+DQ+", "+DQ+"editor"+DQ+", "+DQ+"viewer"+DQ+");  // FIRST");
p("    const body = await request.json();");
p("    const parsed = SearchSchema.safeParse(body);");
p("    if (!parsed.success) {");
p("      return NextResponse.json(");
p("        { success: false, error: { code: "+DQ+"VALIDATION_ERROR"+DQ+", message: "+DQ+"Invalid input"+DQ+", details: parsed.error.flatten() } },");
p("        { status: 400 }");
p("      );");
p("    }");
p("    const context = await searchOnyx(parsed.data.query, parsed.data.maxResults);");
p("    return NextResponse.json({ success: true, data: context });");
p("  } catch (error) {");
p("    const message = error instanceof Error ? error.message : "+DQ+"Unknown error"+DQ+";");
p("    if (message === "+DQ+"AUTH_REQUIRED"+DQ+") {");
p("      return NextResponse.json(");
p("        { success: false, error: { code: "+DQ+"AUTH_REQUIRED"+DQ+", message: "+DQ+"Authentication required"+DQ+" } },");
p("        { status: 401 }");
p("      );");
p("    }");
p("    if (message === "+DQ+"AUTH_FORBIDDEN"+DQ+") {");
p("      return NextResponse.json(");
p("        { success: false, error: { code: "+DQ+"AUTH_FORBIDDEN"+DQ+", message: "+DQ+"Admin access required"+DQ+" } },");
p("        { status: 403 }");
p("      );");
p("    }");
p("    if (message === "+DQ+"ONYX_UNAVAILABLE"+DQ+") {");
p("      return NextResponse.json(");
p("        { success: false, error: { code: "+DQ+"ONYX_UNAVAILABLE"+DQ+", message } },");
p("        { status: 503 }");
p("      );");
p("    }");
p("    return NextResponse.json(");
p("      { success: false, error: { code: "+DQ+"INTERNAL_ERROR"+DQ+", message } },");
p("      { status: 500 }");
p("    );");
p("  }");
p("}");
p(B3);
p("");
p("---");
p("");
p("## 12. File Reference Map");
p("");
p("All files examined during this analysis:");
p("");
p("### API Routes");
p("- src/app/api/health/route.ts");
p("- src/app/api/auth/[...nextauth]/route.ts");
p("- src/app/api/users/route.ts");
p("- src/app/api/users/[id]/route.ts");
p("- src/app/api/content-map/route.ts");
p("- src/app/api/content-map/[id]/route.ts");
p("- src/app/api/content-map/import/route.ts");
p("");
p("### Lib Modules");
p("- src/lib/auth/config.ts");
p("- src/lib/auth/session.ts");
p("- src/lib/auth/password.ts");
p("- src/lib/db/index.ts");
p("- src/lib/db/retry.ts");
p("- src/lib/env.ts");
p("- src/lib/onyx/client.ts");
p("- src/lib/claude/client.ts");
p("- src/lib/cloudinary/client.ts");
p("- src/lib/content-map/index.ts");
p("- src/lib/content-map/import.ts");
p("- src/lib/content-map/slug.ts");
p("");
p("### Types");
p("- src/types/index.ts");
p("- src/types/api.ts");
p("- src/types/onyx.ts");
p("- src/types/auth.ts");
p("");
p("### Infrastructure");
p("- src/middleware.ts");
p("- prisma/schema.prisma");
p("- .env.example");
p("");
p("### Test Scripts");
p("- scripts/test-guide-1.ts");
p("- scripts/test-guide-2.ts");
p("");
p("### Generated Docs");
p("- docs/_generated/api-routes.md");
p("- docs/_generated/env-vars.md");
p("- docs/_generated/prisma-models.md");

fs.writeFileSync("pattern-finder-findings.md", out.join(NL));
console.log("Written: pattern-finder-findings.md (" + out.length + " lines)");
